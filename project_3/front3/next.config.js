/**
 * next.config.js
 * ------------------------------------------------------------------
 * ★ eslint.ignoreDuringBuilds: true
 *   원본 프로젝트(boot1 기반)는 airbnb 스타일가이드(.eslintrc)를 쓰고 있는데,
 *   실제 코드 스타일은 그 규칙을 전부 지키도록 작성되어 있지 않습니다
 *   (따옴표 종류, 들여쓰기 칸수, 세미콜론 등 - 전부 "스타일" 문제일 뿐 실제
 *   동작에는 영향이 없습니다). 예전에는 ESLint 파서(babel-eslint, deprecated)가
 *   옵셔널 체이닝 등 최신 문법에서 내부적으로 크래시가 나서 이 규칙들이 사실상
 *   적용되지 않고 있었는데, 파서를 최신(@babel/eslint-parser)으로 교체하면서
 *   그동안 가려져 있던 수백 건의 스타일 위반이 전부 드러나 빌드 자체가 막혀버렸습니다.
 *   실제 "코드가 잘못됐는지"는 Next.js 의 컴파일(babel/webpack) 단계에서 이미
 *   충분히 검증되므로, 스타일 규칙 위반 때문에 프로덕션 빌드 자체가 실패하지
 *   않도록 여기서 분리합니다. (에디터/CI에서 `npx eslint .` 는 여전히 정상 동작하며
 *   스타일 위반을 그대로 보여줍니다 - 빌드만 막지 않을 뿐입니다)
 * ------------------------------------------------------------------
 */
module.exports = {
  reactStrictMode: true,
  eslint: {
    ignoreDuringBuilds: true,
  },
};
